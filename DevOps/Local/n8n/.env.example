# Copy to .env and keep .env out of git.
# Generate a stable key once, then never change it (credentials depend on it):
#   openssl rand -hex 24

N8N_ENCRYPTION_KEY=replace-with-a-stable-32-plus-char-secret
